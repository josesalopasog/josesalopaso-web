import "./styles.css";

const Hero = () => {
    return (
        <section className="hero-section" id="hero-section">
            <h1>Hero Section</h1>
        </section>
    );
};

export default Hero;
